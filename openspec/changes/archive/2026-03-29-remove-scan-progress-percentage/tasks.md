## 1. 簡化 CLI 進度顯示

- [x] 1.1 從 `cmd/avengine/main.go` 移除 `CountFiles` 呼叫與 `total` 變數
- [x] 1.2 從 `onProgress` 閉包中移除 `if total > 0` 分支及百分比計算（`pct`）
- [x] 1.3 將進度格式字串統一改為 `\r[%d] %s`（僅顯示檔案數目與截斷路徑）

## 2. 驗證正確性

- [x] 2.1 執行現有測試套件（`go test ./...`）並確認全數通過
- [x] 2.2 手動驗證：在文字模式下執行掃描，確認輸出顯示 `[N] path` 且無百分比
- [x] 2.3 確認掃描完成後進度行仍在報告印出前被清除
